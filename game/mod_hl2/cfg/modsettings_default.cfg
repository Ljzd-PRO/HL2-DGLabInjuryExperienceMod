//------------------------------------------------------------------------------
//
// Loaded by the engine on startup right before loading modsettings.cfg.
//
// NOTE: modsettings.cfg loading does not fall through filesystem, so ep2 will not load ep1's settings if ep2
//        doesn't specify a modsettings.cfg. defaultsettings.cfg does.
//
//------------------------------------------------------------------------------

mat_bloom_scalefactor_scalar 1
mat_specular_scalar -1

